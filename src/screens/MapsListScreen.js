import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const MapsListScreen = ({ navigation }) => {
	return (
		<SafeAreaView>
			<Text>Maps List Screen</Text>
		</SafeAreaView>
	);
}

export default MapsListScreen;